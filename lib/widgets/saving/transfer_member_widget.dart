import 'package:flutter/material.dart';

class TransferMemberWidget extends StatelessWidget {
  const TransferMemberWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
